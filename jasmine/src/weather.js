Weather = function() {
  this._weather = " "


};

  Weather.prototype.forecast = function(){
    var meteo = [];

    meteo = ["stormy","sunny","sunny","sunny"]
     this._weather = "sunny"; //meteo[Math.floor (Math.random() * meteo.length)];
    };
